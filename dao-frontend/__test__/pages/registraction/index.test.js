import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import Registration from '../../../src/pages/registraction';

test('renders component', () => {
    render(<Registration />);

});
